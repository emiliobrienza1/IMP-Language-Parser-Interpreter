# IMP Language Parser & Interpreter

A Haskell command-line interpreter for **IMP**, a small imperative programming language used to demonstrate parsing, abstract syntax trees, evaluation semantics, state management, and file-based program execution.

The project tokenises IMP source input, parses it into an abstract syntax tree, evaluates integer expressions, Boolean expressions and commands, and outputs the resulting program state.

